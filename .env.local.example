## Optional for local development:
## - Set BASIC_AUTH_ENABLED=false (or omit) to disable protection locally.
## - Keep real secrets in `.env.local` (which is gitignored).
BASIC_AUTH_ENABLED=true
BASIC_AUTH_USER=interviewer
BASIC_AUTH_PASS=change-me
